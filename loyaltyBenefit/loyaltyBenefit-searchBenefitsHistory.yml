info:
  name: /loyaltyBenefit/searchBenefitsHistory
  type: http
  seq: 2

http:
  method: POST
  url: "{{hostname}}/loyaltyBenefit/searchBenefitsHistory"
  body:
    type: json
    data: |-
      {
        "membershipReference": {
          "id": "{{custNo}}",
          "type": "CUSTNO"
        },
        "fromDate": "2026-08-10",
        "toDate": "2026-08-15"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
