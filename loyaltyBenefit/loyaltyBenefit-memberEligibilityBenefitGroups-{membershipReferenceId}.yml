info:
  name: /loyaltyBenefit/memberEligibilityBenefitGroups/{membershipReferenceId}
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/loyaltyBenefit/memberEligibilityBenefitGroups/{{custNo}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
