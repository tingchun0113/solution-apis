info:
  name: /accrual/genericEvent
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/accrual/genericEvent"
  body:
    type: json
    data: |-
      {
        "membershipReference": {
          "id": "{{custNo}}",
          "type": "CUSTNO"
        },
        "externalCode": "{{$randomUUID}}",
        "partner": "{{PARTNER_STANDARD}}",
        "accrualCode": "{{accrualCode}}",
        "value": 10000,
        "actionType": "CREDIT"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
