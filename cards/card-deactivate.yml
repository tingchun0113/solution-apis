info:
  name: /card/deactivate
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/card/deactivate"
  body:
    type: json
    data: |-
      {
        //   "membershipReference": {
        //     "id": "{{custNo}}",
        //     "type": "CUSTNO"
        //   },
        "ffpMemberNumber": {{custNo}},
        "cardType": "AEP"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
