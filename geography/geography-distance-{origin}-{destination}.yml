info:
  name: /geography/distance/{origin}/{destination}
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/geography/distance/MUC/TPE"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
