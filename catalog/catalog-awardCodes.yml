info:
  name: /catalog/awardCodes
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/catalog/awardCodes"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
