info:
  name: /catalog/{catalogName}/entries
  type: http
  seq: 3

http:
  method: GET
  url: "{{hostname}}/catalog/CARD_TYPE/entries"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
