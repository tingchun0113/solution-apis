info:
  name: /catalog/accrualCodes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/catalog/accrualCodes"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
