info:
  name: /catalog/{categoryName}/entry/{catalogEntryCode}
  type: http
  seq: 4

http:
  method: GET
  url: "{{hostname}}/catalog/CARD_TYPE/entry/AEP"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
